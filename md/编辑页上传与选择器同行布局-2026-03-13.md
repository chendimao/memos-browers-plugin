# 编辑页上传与选择器同行布局实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**功能目标：** 在 `feat/figma-restyle-pages-20260227` worktree 中调整编辑页布局，使图片上传、附件上传、标签选择、可见性选择和提交按钮保持在同一行显示。

**技术方案 / 架构说明：**
本次仅调整编辑页现有 flex 布局，不改动 Vue 组件结构和交互逻辑。通过重新定义 `action-tools`、`left-tools`、`right-tools` 的伸缩策略，并限制右侧各子组件的占宽方式，让上传按钮组固定宽度、标签选择器占据剩余空间、可见性选择器与提交按钮保留稳定宽度，从而实现单行布局。

**Tech Stack:** Vue 3、Vite、SFC scoped CSS

---

### 任务 1：确认布局瓶颈

**Files:**
- Modify: `/Users/chendimao/WWW/memos-browers-plugin/.worktrees/feat-figma-restyle-pages-20260227/src/App.vue`
- Reference: `/Users/chendimao/WWW/memos-browers-plugin/.worktrees/feat-figma-restyle-pages-20260227/src/components/TagSelector.vue`
- Reference: `/Users/chendimao/WWW/memos-browers-plugin/.worktrees/feat-figma-restyle-pages-20260227/src/components/CustomSelect.vue`

**步骤任务列表：**
1. 检查编辑页模板中 `action-tools`、`left-tools`、`right-tools` 的结构。
2. 检查 `TagSelector` 与 `CustomSelect` 的默认宽度设置。
3. 确认只需要 CSS 调整即可完成单行布局，不需要改组件逻辑。

### 任务 2：实现单行布局

**Files:**
- Modify: `/Users/chendimao/WWW/memos-browers-plugin/.worktrees/feat-figma-restyle-pages-20260227/src/App.vue`

**步骤任务列表：**
1. 将 `action-tools` 改为不换行的同排布局。
2. 让 `left-tools` 固定宽度，不参与挤压。
3. 让 `right-tools` 占据剩余空间并禁止换行。
4. 让标签选择器占满右侧可伸缩空间。
5. 让可见性选择器和提交按钮保持固定或半固定宽度。

### 任务 3：验证

**Files:**
- Modify: `/Users/chendimao/WWW/memos-browers-plugin/.worktrees/feat-figma-restyle-pages-20260227/src/App.vue`

**步骤任务列表：**
1. 运行 `npm run build` 确认构建通过。
2. 检查输出是否仍正常生成 `dist/`。
3. 如用户需要，再把最新 `dist/` 复制到当前项目根目录 `export/`。

